# Use the official Node.js Alpine image (small and efficient)
FROM node:18-alpine

# Enable Corepack and prepare PNPM (official way to install PNPM)
RUN corepack enable && corepack prepare pnpm@latest --activate

# Set working directory
WORKDIR /app

# Copy package files first (to leverage Docker caching)
COPY package.json pnpm-lock.yaml ./

# Install dependencies using PNPM (without modifying lockfile)
RUN pnpm install --frozen-lockfile

# Copy the rest of the app files
COPY . .

# Expose the port for Next.js
EXPOSE 3000

# Set the default command to start the app
CMD ["pnpm", "dev"]